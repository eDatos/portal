describe("CommentView", function () {

    var CommentView = STAT4YOU.modules.comments.CommentView;

    /*
    it("Replace comment link", function () {
        var commentView = new CommentView();

        expect(commentView._replaceCommentLinks("hola #1 esto es un comentario"))
            .toEqual('hola <a href="#1">#1</a> esto es un comentario');

        expect(commentView._replaceCommentLinks("hola#1 esto es un comentario"))
            .toEqual('hola<a href="#1">#1</a> esto es un comentario');

        expect(commentView._replaceCommentLinks("#1 esto es un comentario"))
            .toEqual('<a href="#1">#1</a> esto es un comentario');

        expect(commentView._replaceCommentLinks("hola #1 esto es un comentario#2#3"))
            .toEqual('hola <a href="#1">#1</a> esto es un comentario<a href="#2">#2</a><a href="#3">#3</a>');

    });*/


});